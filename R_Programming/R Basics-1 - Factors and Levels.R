# 1. Create a vector variable with the values "yes", "no", "maybe".
# 2. Display the levels in this variable.

vector_variable <- c('yes','no','yes','maybe')
v_variable <- factor(vector_variable)
print(v_variable)